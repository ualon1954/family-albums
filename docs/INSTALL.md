# Installation
1. Create a blank Google Sheet.
2. Open Extensions → Apps Script from that sheet.
3. Add all files in `backend/`.
4. Run `setupDatabase`.
5. Deploy as Web app.
6. Put the deployment `/exec` URL in `frontend/js/config.js`.
7. Test `?action=health`.
